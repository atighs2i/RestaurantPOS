odoo.define('restaurant_pos_custom.pos_custom', function(require){
    'use strict';

    var PosModel = require('point_of_sale.models');
    // placeholder for JS extensions (KDS, language toggles)
    console.log('restaurant_pos_custom loaded');
});
